import React from "react"

type NataPacksListPropsType = {}

export const NataPacksList = React.memo((props: NataPacksListPropsType) => {
    return (
        <>
            {/* Применяй стили из PacksList.module.scss */}
        </>
    )
})